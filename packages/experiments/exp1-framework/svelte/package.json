{
  "name": "@experiments/exp1-svelte",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "size": "vite build && gzip -c dist/assets/*.js | wc -c && gzip -c dist/assets/*.css 2>/dev/null | wc -c || echo 0"
  },
  "dependencies": {
    "fusain": "workspace:*"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^5.0.0",
    "svelte": "^5.0.0",
    "vite": "^6.0.0"
  }
}
