{
  "name": "@experiments/exp1-preact",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "size": "vite build && gzip -c dist/assets/*.js | wc -c && gzip -c dist/assets/*.css 2>/dev/null | wc -c || echo 0"
  },
  "dependencies": {
    "fusain": "workspace:*"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.9.0",
    "preact": "^10.25.0",
    "vite": "^6.0.0"
  }
}
